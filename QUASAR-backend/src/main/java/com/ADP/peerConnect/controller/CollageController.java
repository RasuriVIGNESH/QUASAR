package com.ADP.peerConnect.controller;

// Legacy wrapper kept for backward compatibility; delegates to CollegeController endpoints via same paths
public class CollageController {
    // Intentionally empty - kept to satisfy any compiled references to the old class name.
}
